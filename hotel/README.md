# Hotel vertical

- **`hotel-page-sythesization.html`** — Hotel landing page (uses `./hotel-landing.css`; isolated from `../styles.css` so warehouse/datacenter pages stay unchanged).
- **`hotel-landing.css`** — Premium hotel-only styles, typography, and layout.
- **`hotel-usecase-section.svg`** — Legacy illustrated map (same themes as the in-page “clarity” section); not embedded in `hotel-page-sythesization.html` anymore—kept as an optional asset.
- **`hotel-workflow-fit.svg`** — Legacy circular workflow diagram; not embedded in `hotel-page-sythesization.html` anymore (replaced by the in-page **workflow fit** section)—kept as an optional asset.
- **`hotel-radio-visibility-section.svg`** — Standalone asset co-located with this vertical; the radio/visibility block in the HTML page is implemented as editorial copy + icons, not via this file.
